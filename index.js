/* ==========================================================
   INDEX.JS - Logica de index.html
   ==========================================================
   Esta pagina (landing) es solo informativa: no tiene
   formularios ni botones que necesiten JavaScript propio. Este
   archivo se deja como espacio reservado por si en el futuro se
   agrega alguna interaccion (por ejemplo, un menu hamburguesa
   para celulares).

   Requiere que comun.js este cargado ANTES que este archivo,
   que se encarga de limpiar mensajes viejos si el usuario
   vuelve a esta pagina con el boton "Atras" del navegador.
   ========================================================== */
